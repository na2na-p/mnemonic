# CD for Main Branch
# main ブランチへのプッシュ時に CI を実行し、成功時にリリース準備

name: CD

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  app-ci:
    name: App CI
    uses: ./.github/workflows/app-ci.yml
    secrets: inherit

  app-ci-go:
    name: App CI (Go)
    uses: ./.github/workflows/app-ci-go.yml
    secrets: inherit

  summary:
    name: Summary
    needs:
      - app-ci
      - app-ci-go
    runs-on: ubuntu-latest
    if: always()

    steps:
      - name: Generate summary
        run: |
          echo "## CD Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Job | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-----|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| App CI | ${{ needs.app-ci.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| App CI (Go) | ${{ needs.app-ci-go.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Triggered by:** ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
